# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies / build output
node_modules/
.next/
out/
dist/

# Personal data — keep local, never commit
docs/Ori_Shalom_CV.pdf

# OS / editor
.DS_Store
*.log
